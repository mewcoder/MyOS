---
id: fav-001
scope: myfav-capture
status: done
depends-on: []
---

# objective

实现 `fav` Skill、Pi Skill 资源加载、`myos fav` dry-run，以及网站/GitHub/文章到本地 MyFav clone 的原子写入闭环。严格遵守现有 MyFav 字段结构；不实现前端、Utterances、自动 push、持久浏览器 profile。

# context

- `docs/INDEX.md`
- `docs/myfav/README.md`
- `docs/myfav/fav.md`
- `docs/plan/analysis/fav-capture.md`

# path

- `skills/fav/`
- `src/agent/pi-adapter.ts`
- `src/config/schema.ts`
- `src/gateway/server.ts`
- `src/index.ts`
- `src/types.ts`
- `src/fav/`
- `src/inbox/service.ts`
- `tests/fav/`
- `README.md`
- `docs/myfav/README.md`
- `docs/myfav/fav.md`
- `docs/INDEX.md`
- `docs/plan/README.md`
- `docs/plan/analysis/fav-capture.md`
- `docs/plan/tasks/fav-001.md`

# constraints

- `src/config/schema.ts` 与 `src/types.ts` 只纳入既有 `skillDir` 改动，不对这两处做其他修改。
- 使用现有依赖，不引入数据库、对象存储或浏览器下载。
- 使用 skill-creator 的 `init_skill.py` 初始化 `skills/fav`，生成 `agents/openai.yaml`，不创建 Skill README。
- `--dry-run` 不得写文件或调用 Git。
- Git commit 只 stage 本次生成的明确路径，不得使用 `git add -A`。
- 不自动 build、lint、push 或打开真实浏览器。

# verification

- 运行 `npm run check`。
- 运行 `fav` 相关的定向 Vitest；网络层使用 fixture/mock，不启动真实浏览器。
- 运行 skill-creator 的 `quick_validate.py skills/fav`。
- 用 Pi `DefaultResourceLoader` 的真实加载路径验证 `fav` 被发现。
- 对用户允许的真实链接仅运行 HTTP dry-run smoke test；网络限制或站点变化需如实记录，不得写 MyFav。

## delivery

- `npm run check`
- `npx vitest run tests/fav`
- `quick_validate.py skills/fav`
- Pi `DefaultResourceLoader` 定向测试实际发现内置 `fav`，缺失的自定义目录产生 diagnostic。
- 真实 HTTP dry-run：`https://linear.app/` 通过 HTTP 提取为 article（3983 characters / 22 images）；`https://github.com/cloudflare/skills` 通过 GitHub API 提取为 repo。两者均未启动 Chrome、未创建 MyFav 目录。
- X、微信公众号和独立博客未在本任务中运行真实网络 smoke；对应层级由 fixture/mock 覆盖。
